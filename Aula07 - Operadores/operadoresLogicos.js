/* 
Operadores Lógicos:

&& => AND => E              * Todas as condições precisam ser verdadeira para retornar true.
|| => OR => OU              * Uma das condiçoes precisa ser verdadeira para retornar true.
! => NOT => NÂO             * Negação de uma expressão.

*/

const exp = true || true || false || true;
console.log(exp);
