//Produto -> aumento, desconto
camiseta = cor, caneca = material

function pessoa(nome,sobrenome,idade) {
   return {
      nascimento() {
         const dataAtual = new Date();
         return dataAtual.get
      }
   }
}

